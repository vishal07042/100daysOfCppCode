#include<iostream>
using namespace std;
#include<vector> 

int main()
{
    // vector <int>  p;
    // p.push_back(2);
    // p.emplace_back(3);
    // // cout<<p[1];


    // vector<pair<int ,int>> vecc;
    // vecc.push_back({2,3});
    // vecc.push_back({4,5});

    // cout<<vecc[1].second;

    vector <int > p(5,20);
    vector <int > q(p);
  cout<<p.empty();


  
   
    return 0;
}